-- Ashen: a warm, muted colorscheme (red/orange, grayscale, hints of teal)
-- https://codeberg.org/ficd/ashen.nvim

---@type LazySpec
return {
  "ficcdaf/ashen.nvim",
  lazy = false,
  priority = 1000,
}
